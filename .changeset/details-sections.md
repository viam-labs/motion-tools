---
'@viamrobotics/visualization': major
---

Add `useDetailsSection` so plugins contribute per-entity content to the details cards, and remove the `DetailsPortal` export along with the unused details portal targets, which never had a working landing zone
