---
'@viamrobotics/visualization': major
---

Move build mode into the opt-in `BuildFrames` plugin, which now also owns the transform-mode and custom-geometry hotkeys, and hide the mode toggle when monitor is the only reachable mode
