---
'@viamrobotics/motion-tools': minor
---

Publish immersive session state as `useEnvironment().current.isImmersive` so only the XR plugin needs `@threlte/xr`, and make the plugin-only peers `@threlte/xr`, `@threlte/rapier`, and `@dimforge/rapier3d-compat` optional
