---
'@viamrobotics/motion-tools': minor
---

Auto-open the Details panel whenever a new entity enters the selection set. Each new selection (lasso or ellipse) points `selectedEntity` at the latest entity, so the panel always reflects the most recent selection.
