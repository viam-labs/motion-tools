# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
.changeset

# Protobuf
draw/v1
src/lib/buf

# Docs
docs/.astro
docs/public/playground
docs/dist
docs/src/content/docs/api

# Snapshots
draw/__snapshots__
e2e/*.ts-snapshots
static/test-fixtures

# A captured motion plan is two JSON objects concatenated with no separator, so
# it is not valid JSON and prettier cannot round-trip it.
src/lib/plugins/MotionPlanReplayer/__tests__/__fixtures__/plan.json
src/lib/plugins/MotionPlanReplayer/__tests__/__fixtures__/salad-plan.json

# IDE
.cursor

# >>> claude-config (managed) >>>
# Claude Code (managed by claude-config; byte-diffed by `claude-config doctor`)
.claude/
.mcp.json
# <<< claude-config (managed) <<<
